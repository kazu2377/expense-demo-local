# Expense Demo ローカル版

`https://www.expense-demo.com/` の画面構造をできる限り維持し、経費登録データの保存先をAWS/RDSからブラウザの `localStorage` へ変更したローカル版です。

## 起動方法

1. Node.js 22.13以上をインストールします。
2. このフォルダーでPowerShellまたはターミナルを開きます。
3. 初回のみ `npm ci` を実行します。
4. `npm run dev` を実行します。
5. ブラウザで `http://localhost:3000/` を開きます。

終了するときは、起動したターミナルで `Ctrl+C` を押します。

## 保存データ

- 登録した経費は、ブラウザの `localStorage` に保存されます。
- トップ画面の「経費を登録する」をクリックすると、それまでの登録データをすべて消去し、レコードなしの状態から開始します。
- 保存キーは `expense_demo_ledgers` です。
- 同じPCでも、ブラウザやブラウザプロファイルが異なるとデータは共有されません。
- ブラウザのサイトデータを削除すると、登録データも削除されます。
- 以前のCookie版で保存されたデータがある場合、初回表示時に `localStorage` へ自動移行します。

## 維持しているHTML情報

経費登録フォームの主要な `form`、`input`、`select`、`label`、`id`、`name`、`class`、一覧表の構造を元サイトに合わせています。保存処理のみブラウザ内へ差し替えています。

## ご利用にあたって

各画面のフッターに、本プログラムの提供目的、お問い合わせ対応、および利用者の責任についての注意事項を表示します。
