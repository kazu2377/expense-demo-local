# Expense Demo localStorage Clone

`https://www.expense-demo.com/` のローカル動作版です。

- 元サイトの主要なHTMLタグ、`id`、`name`、`class`を維持
- AWS/RDSへの保存をブラウザの`localStorage`へ変更
- サーバーや外部データベースへの入力データ送信なし
- トップ画面から経費登録を開始するたびに登録データを初期化
- 画面上のサービス名は「Expense Demo」

日本語の起動方法とデータ保存仕様は、`LOCAL_SETUP_JA.md`を参照してください。
